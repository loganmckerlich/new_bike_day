[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt --break-system-packages"]

[phases.build]
cmds = []

[start]
cmd = "uvicorn webhook:app --host 0.0.0.0 --port $PORT"